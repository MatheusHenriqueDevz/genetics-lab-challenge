version: '3.8'

services:
  api-genetica:
    build: ./genetics-back
    container_name: genetics-backend
    ports: 
      - "3333:3333"
  
  web-genetica:
    build: ./genetics-front
    container_name: genetics-frontend
    ports:
      - "5173:5173"
    
    depends_on:
      - api-genetica